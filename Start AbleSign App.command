#!/usr/bin/env bash
# Double-click this file in Finder to start the app - no Terminal typing needed.
cd "$(dirname "${BASH_SOURCE[0]}")"
bash start_app.sh
echo ""
read -p "Press Enter to close this window..."
